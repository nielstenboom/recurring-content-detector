# action.yml
name: 'test-docker'
on: 'pull_request'
jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    steps:

    - uses: actions/checkout@v3

    - name: Setup Docker buildx
      uses: docker/setup-buildx-action@v1

    - run: |
        docker build -t testbuild .
        docker run -it -v "$(pwd)":/app testbuild pytest /app/tests